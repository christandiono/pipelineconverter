package Parser;

/**
 * Take a stream and parse into the generic structure.
 * Should take a structure object with all the rules, stream
 * Use the structure rules to generate structure from file.
 * 
 */
public interface Parser {

}
